spring:
  cloud:
    stream:
      bindings:
        input:
          destination: TWEETS.Q
          group: BOSSIDEAS.<ATTENDEE_NAME>
          binder: solaceBroker
      binders:
        solaceBroker:
          type: solace
          environment:
            solace:
              java:
                host: tcp://localhost:55555
                msgVpn: default
                clientUsername: default
                clientPassword: default
                connectRetries: 3
                connectRetriesPerHost: 0
                reconnectRetries: 3
      solace:
        bindings:
          input:
            consumer:
              queueAdditionalSubscriptions: tweets/stream/<ATTENDEE_NAME>/features
logging:
  level:
    root: INFO
    com.solacesystems.jcsmp: INFO
    org.springframework: INFO

server:
  port: 8098
